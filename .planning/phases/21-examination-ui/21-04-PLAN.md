---
phase: 21-examination-ui
plan: 04
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
  - 21-03
files_modified:
  - src/app/(dashboard)/rbia/page.tsx
  - src/app/(dashboard)/rbia/module/[moduleCode]/page.tsx
autonomous: true
requirements:
  - EXAM-01
  - EXAM-02
  - EXAM-07
  - EXAM-08

must_haves:
  truths:
    - "RBIA engagement dashboard page at /rbia shows score panel + module grid with live data from DAL"
    - "Per-module examination page at /rbia/module/[moduleCode] shows sticky header + filter bar + examination tree with live data"
    - "Auditor navigating from module card to tree page sees the full examination tree for that module"
    - "Page refresh on the module tree page restores expand state from URL params"
    - "All previously saved scores display correctly when reopening the examination page (EXAM-09 persistence verification)"
  artifacts:
    - path: "src/app/(dashboard)/rbia/page.tsx"
      provides: "RBIA engagement dashboard server page assembling score panel + module grid"
      min_lines: 30
    - path: "src/app/(dashboard)/rbia/module/[moduleCode]/page.tsx"
      provides: "Per-module examination tree server page"
      min_lines: 30
  key_links:
    - from: "src/app/(dashboard)/rbia/page.tsx"
      to: "getEngagementModuleScores"
      via: "DAL call in server component"
      pattern: "getEngagementModuleScores"
    - from: "src/app/(dashboard)/rbia/module/[moduleCode]/page.tsx"
      to: "getExaminationTree"
      via: "DAL call in server component"
      pattern: "getExaminationTree"
    - from: "src/app/(dashboard)/rbia/page.tsx"
      to: "RbiaScorePanel"
      via: "component import"
      pattern: "RbiaScorePanel"
    - from: "src/app/(dashboard)/rbia/page.tsx"
      to: "RbiaModuleGrid"
      via: "component import"
      pattern: "RbiaModuleGrid"
    - from: "src/app/(dashboard)/rbia/module/[moduleCode]/page.tsx"
      to: "RbiaExaminationTree"
      via: "component import"
      pattern: "RbiaExaminationTree"
---

<objective>
Assemble the RBIA engagement dashboard and per-module examination pages by wiring the components from Plans 21-01, 21-02, and 21-03 to server-side data fetching.

Purpose: This plan connects the UI components to real data — making the examination workflow fully functional end-to-end. The engagement dashboard shows overall progress and the module tree page is the primary auditor workspace.

Output: Two server component pages that load data from DAL functions and render the Phase 21 client components.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-examination-ui/21-01-SUMMARY.md
@.planning/phases/21-examination-ui/21-02-SUMMARY.md
@.planning/phases/21-examination-ui/21-03-SUMMARY.md
@src/data-access/rbia-examination.ts
@src/data-access/rbia-scoring.ts
@src/app/(dashboard)/rbia/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RBIA engagement dashboard page</name>
  <files>src/app/(dashboard)/rbia/page.tsx</files>
  <action>
Replace the Phase 19 stub `/rbia/page.tsx` with the full RBIA engagement dashboard server component.

This page receives the engagementId from the URL search params (the engagement gateway from Phase 19 redirects here with `?engagementId=...`). Check how the existing stub page receives the engagementId — it may be via search params or via a parent layout. Follow the existing pattern.

**Server component data loading:**

1. `const session = await getRequiredSession()` — from `@/data-access/session`
2. `const moduleScores = await getEngagementModuleScores(session, engagementId)` — from `@/data-access/rbia-scoring`
3. `const moduleSelections = await getModuleSelections(session, engagementId)` — from `@/data-access/rbia-examination`
4. `const branchScore = await getEngagementBranchScore(session, engagementId)` — from `@/data-access/rbia-scoring`
5. Load engagement details to get `engagementStatus` — use existing DAL function (check `src/data-access/` for engagement detail query)

**Page layout:**

1. Page title/breadcrumb: "RBIA Examination — [Branch Name]"
2. `<RbiaScorePanel>` — composite score display (from Plan 21-03)
3. `<RbiaModuleGrid>` — module cards grid (from Plan 21-01)
4. Pass all fetched data as props to client components

**Error handling:**

- If engagementId is missing, redirect to audit execution list
- If engagement is not RBIA type, redirect to legacy sections path (should not happen due to gateway, but guard)
- Show loading skeleton while data loads (use `<Suspense>` with skeleton fallback if needed)

Follow existing page patterns in `src/app/(dashboard)/` for layout, imports, and auth checking.
</action>
<verify>
<automated>cd /Users/admin/Developer/AEGIS && npx tsc --noEmit src/app/\(dashboard\)/rbia/page.tsx 2>&1 | head -30</automated>
<manual>Navigate to /rbia?engagementId=... and verify score panel + module grid render with real data</manual>
</verify>
<done>RBIA engagement dashboard page replaces stub, fetches data from DAL, renders RbiaScorePanel and RbiaModuleGrid with live data</done>
</task>

<task type="auto">
  <name>Task 2: Build per-module examination tree page</name>
  <files>src/app/(dashboard)/rbia/module/[moduleCode]/page.tsx</files>
  <action>
Create the per-module examination tree page as a server component.

**Route:** `/rbia/module/[moduleCode]` with `engagementId` from search params.

**Server component data loading:**

1. `const session = await getRequiredSession()`
2. `const { moduleCode } = await params` — dynamic route param (Next.js 16 uses `await params`)
3. `const engagementId = (await searchParams).engagementId` — from URL search params
4. `const tree = await getExaminationTree(session, engagementId)` — full tree
5. Filter tree to find the module node matching `moduleCode` and extract its children as the module-specific tree
6. `const moduleScores = await getEngagementModuleScores(session, engagementId)` — to find this module's score data
7. Extract the specific module's score row for the sticky header

**Page layout:**

1. Breadcrumb: "RBIA Examination > [Module Name]" with back link to `/rbia?engagementId=...`
2. `<Suspense fallback={<TreeSkeleton />}>` wrapping the client component (required for `useSearchParams` in client component)
3. `<RbiaExaminationTree>` — the full tree component (from Plan 21-02)
4. Pass props: `tree` (module's children nodes), `engagementId`, `initialExpanded` (from `searchParams.expanded ?? ""`), `moduleName`, `moduleScore` (extracted from moduleScores)

**Tree skeleton:**

- Simple loading placeholder: a Card with 10 animated rows of varying indent levels
- Use shadcn/ui `<Skeleton>` components

**Important Next.js 16 patterns:**

- `params` and `searchParams` are Promises in Next.js 16 — must use `await params` and `await searchParams`
- Wrap client component in `<Suspense>` for `useSearchParams` compatibility per RESEARCH.md Pitfall 2
- Pass `initialExpanded` as a prop from server to avoid Suspense issues

Follow existing page patterns for breadcrumbs, layout containers, and auth.
</action>
<verify>
<automated>cd /Users/admin/Developer/AEGIS && npx tsc --noEmit src/app/\(dashboard\)/rbia/module/\[moduleCode\]/page.tsx 2>&1 | head -30</automated>
<manual>Navigate to /rbia/module/MOD01?engagementId=... and verify tree renders with expand/collapse and score buttons</manual>
</verify>
<done>Per-module examination tree page exists, fetches tree data for specific module, renders RbiaExaminationTree with Suspense boundary, passes initialExpanded from URL params</done>
</task>

</tasks>

<verification>
- Both pages compile with `npx tsc --noEmit`
- Dashboard page renders score panel + module grid
- Module tree page renders examination tree with correct module data
- Navigating from dashboard card to tree page works
- URL params (engagementId, expanded) flow correctly between pages
- Page refresh on tree page restores expand state
</verification>

<success_criteria>
Both pages compile, wire components to DAL data, and form a complete navigation flow: dashboard (score panel + module grid) -> per-module tree (examination + scoring + filtering).
</success_criteria>

<output>
After completion, create `.planning/phases/21-examination-ui/21-04-SUMMARY.md`
</output>
