---
phase: 01-project-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/demo/findings.json
  - src/data/demo/compliance-calendar.json
  - src/data/index.ts
autonomous: true

must_haves:
  truths:
    - "findings.json contains 35 findings with realistic RBI-style observations"
    - "Compliance calendar data contains upcoming deadlines for regulatory submissions"
    - "All demo data exports work via src/data/index.ts"
  artifacts:
    - path: "src/data/demo/findings.json"
      provides: "35 audit findings with severity distribution"
      contains: "FND-035"
    - path: "src/data/demo/compliance-calendar.json"
      provides: "Compliance calendar events with upcoming deadlines"
      min_lines: 50
    - path: "src/data/index.ts"
      provides: "Barrel exports for all data including new calendar data"
      exports: ["complianceCalendar"]
  key_links:
    - from: "src/data/index.ts"
      to: "src/data/demo/compliance-calendar.json"
      via: "export default import"
      pattern: "compliance-calendar"
---

<objective>
Complete the demo data layer by adding 25 more audit findings (total 35) and creating compliance calendar data.

Purpose: Phase 1 requires DATA-04 (35 findings) and DATA-05 (compliance calendar). Currently only 10 findings exist. The compliance calendar is needed for the dashboard regulatory calendar widget and tracks upcoming RBI submission deadlines.

Output: findings.json with 35 entries, new compliance-calendar.json, updated data exports.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/data/demo/findings.json
@src/data/demo/audit-plans.json
@src/data/demo/compliance-requirements.json
@src/data/demo/rbi-circulars.json
@src/data/demo/staff.json
@src/data/index.ts

Key patterns from Quick Task 002:

- Severity distribution: Mix of critical (2-3), high (8-10), medium (12-15), low (8-10) -- realistic for Tier 2 UCB
- Finding IDs: FND-001 through FND-035 sequential
- Timeline entries: 2-5 entries per finding showing audit lifecycle
- Category alignment: Findings reference existing compliance requirement categories
- Audit IDs: Reference existing AUD-001 through AUD-008
- Staff names: Use existing staff from staff.json (Priya Sharma, Amit Joshi, Rajesh Kulkarni, etc.)
- ISO 8601 date strings throughout
- Status values: "identified", "under-review", "responded", "action-planned", "closed"
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add 25 more findings to findings.json (total 35)</name>
  <files>src/data/demo/findings.json</files>
  <action>
    Read the existing 10 findings in src/data/demo/findings.json to understand the exact JSON structure. Then add 25 more findings (FND-011 through FND-035) to the findings array.

    **Severity distribution for the 25 new findings:**
    - Critical: 2 (total ~3-4 across all 35)
    - High: 7 (total ~10)
    - Medium: 10 (total ~14)
    - Low: 6 (total ~8)

    **Category coverage -- spread across these RBI domains:**
    - Credit Risk (5-6 findings): Loan appraisal deficiencies, NPA classification errors, provisioning shortfalls, exposure concentration, credit monitoring lapses
    - Governance & Internal Controls (3-4): Board meeting irregularities, inadequate internal controls, conflict of interest, KMP compliance
    - IT/Cyber Security (3-4): CBS access controls, password policy violations, disaster recovery gaps, data backup issues, cyber incident reporting
    - KYC/AML (3-4): KYC non-compliance for high-value accounts, suspicious transaction reporting delays, CKYC registration gaps
    - Treasury/ALM (2-3): Investment policy violations, ALM mismatch, interest rate risk
    - Operations (2-3): Cash management issues, safe custody, outsourcing controls
    - Regulatory Compliance (2-3): Statutory return delays, violation of exposure norms, DAKSH reporting gaps

    **Audit assignment:** Distribute across existing audit plan IDs (AUD-001 through AUD-008).

    **Auditor assignment:** Use names from staff.json -- Priya Sharma (Chief Audit Officer), Amit Joshi (Senior Auditor), Rajesh Kulkarni (Compliance Officer), Meena Patil (IT Officer), and other staff.

    **Status distribution across all 35:**
    - identified: 5-6
    - under-review: 6-8
    - responded: 8-10
    - action-planned: 6-8
    - closed: 4-5

    **Timeline entries:** 2-5 per finding with realistic dates spanning 2025-06 to 2026-02. Include actors from staff.json.

    **Dates:** All ISO 8601 format. Created dates range from 2025-06-01 to 2026-01-15. Target dates range from 2025-12-01 to 2026-06-30.

    Each finding must have: id, auditId, title, category, severity, status, observation (2-4 sentences with RBI regulatory language), rootCause (1-3 sentences), riskImpact (1-2 sentences), auditeeResponse (1-3 sentences), actionPlan (numbered steps), assignedAuditor, targetDate, createdAt, updatedAt, timeline[].

  </action>
  <verify>
    Run: `python3 -c "import json; f=json.load(open('src/data/demo/findings.json')); print(f'Count: {len(f[\"findings\"])}'); sevs={s: sum(1 for x in f[\"findings\"] if x[\"severity\"]==s) for s in ['critical','high','medium','low']}; print(f'Severity: {sevs}')"` -- should show Count: 35 with reasonable severity distribution.
    Run: `npx tsc --noEmit --pretty` -- should compile.
  </verify>
  <done>
    findings.json contains 35 findings with realistic RBI observations, proper severity distribution, and all required fields populated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create compliance calendar and update data exports</name>
  <files>
    src/data/demo/compliance-calendar.json
    src/data/index.ts
  </files>
  <action>
    1. **Create src/data/demo/compliance-calendar.json** with 15-20 compliance calendar events representing upcoming regulatory deadlines for a UCB.

    Structure:
    ```json
    {
      "calendarEvents": [
        {
          "id": "CAL-001",
          "title": "Monthly Statutory Returns - SLR/CRR",
          "category": "Reporting",
          "dueDate": "2026-02-15",
          "status": "upcoming",
          "description": "Submit monthly SLR and CRR compliance returns to RBI",
          "regulatoryReference": "RBI/2023-24/45",
          "frequency": "monthly",
          "assignedTo": "Rajesh Kulkarni"
        }
      ]
    }
    ```

    **Calendar events should include:**
    - Monthly returns: SLR/CRR reporting, NPA classification, large exposure reports (recurring monthly)
    - Quarterly returns: ALM statement, CRAR computation, sectoral exposure, DAKSH data submission
    - Annual: Statutory audit completion, Board-approved policies review, KYC/AML annual review, IT audit
    - One-time/upcoming: RBI inspection preparation, Annual General Meeting compliance, External auditor appointment
    - Compliance deadlines: Policy reviews (credit policy, investment policy, ALM policy)

    **Status values:** "upcoming" (future), "due-soon" (within 7 days), "overdue" (past due date), "completed"
    **Dates:** Range from 2026-02-01 to 2026-08-31 so the calendar has data for several months ahead.
    **Assigned to:** Use staff names from staff.json.

    2. **Update src/data/index.ts** -- Add export for compliance calendar:
    ```typescript
    export { default as complianceCalendar } from './demo/compliance-calendar.json';
    ```

  </action>
  <verify>
    Run: `python3 -c "import json; f=json.load(open('src/data/demo/compliance-calendar.json')); print(f'Events: {len(f[\"calendarEvents\"])}')"` -- should show 15-20 events.
    Run: `npx tsc --noEmit --pretty` -- should compile without errors.
  </verify>
  <done>
    Compliance calendar JSON exists with 15-20 events covering monthly, quarterly, and annual deadlines. Data exports updated in index.ts.
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; f=json.load(open('src/data/demo/findings.json')); print(len(f['findings']))"` outputs 35
2. `python3 -c "import json; f=json.load(open('src/data/demo/compliance-calendar.json')); print(len(f['calendarEvents']))"` outputs 15+
3. `npx tsc --noEmit --pretty` passes
4. `grep 'complianceCalendar' src/data/index.ts` returns a match
</verification>

<success_criteria>

- findings.json has exactly 35 findings with proper severity distribution
- compliance-calendar.json has 15-20 regulatory deadline events
- All data exports work from src/data/index.ts
- TypeScript compiles without errors
- DATA-04 and DATA-05 requirements are satisfied
  </success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md`
</output>
