---
phase: 02-core-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/select.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/checkbox.tsx
autonomous: true

must_haves:
  truths:
    - "Recharts library available for chart components"
    - "TanStack Table available for sortable/filterable tables"
    - "Additional shadcn/ui primitives (select, tabs, popover, scroll-area, progress, checkbox) available"
  artifacts:
    - path: "package.json"
      provides: "recharts, @tanstack/react-table dependencies"
      contains: "recharts"
    - path: "src/components/ui/select.tsx"
      provides: "Select dropdown component"
    - path: "src/components/ui/tabs.tsx"
      provides: "Tabs component for view switching"
    - path: "src/components/ui/popover.tsx"
      provides: "Popover for calendar widget"
    - path: "src/components/ui/scroll-area.tsx"
      provides: "Scroll area for lists"
    - path: "src/components/ui/progress.tsx"
      provides: "Progress bar component"
    - path: "src/components/ui/checkbox.tsx"
      provides: "Checkbox for filters"
  key_links:
    - from: "package.json"
      to: "node_modules/recharts"
      via: "pnpm install"
      pattern: "recharts"
    - from: "package.json"
      to: "node_modules/@tanstack/react-table"
      via: "pnpm install"
      pattern: "@tanstack/react-table"
---

<objective>
Install all Phase 2 dependencies: Recharts for visualizations, TanStack Table for sortable/filterable tables, and additional shadcn/ui primitives needed by dashboard, compliance, and audit plan components.

Purpose: All Phase 2 plans depend on these libraries and components. Installing them upfront in a dedicated plan prevents dependency issues during component development.
Output: Updated package.json with new dependencies, 6 new shadcn/ui component files.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and TanStack Table</name>
  <files>package.json</files>
  <action>
    Run the following pnpm commands to install Phase 2 dependencies:

    1. `pnpm add recharts @tanstack/react-table`

    Recharts is used for:
    - Dashboard donut chart (DASH-02: audit coverage)
    - Dashboard regulatory calendar widget (DASH-05)
    - Compliance trend chart (COMP-06: health trend last 6 months)

    TanStack Table is used for:
    - Compliance registry table (COMP-01, COMP-02: sortable, filterable columns)

    Do NOT install react-big-calendar — the audit plan calendar will use a custom month-grid component built with Tailwind CSS, which is lighter and better suited to a prototype.

    After install, verify by running `pnpm build` to confirm no dependency conflicts.
  </action>
  <verify>
    - `pnpm ls recharts` shows recharts installed
    - `pnpm ls @tanstack/react-table` shows @tanstack/react-table installed
    - `pnpm build` completes without errors
  </verify>
  <done>recharts and @tanstack/react-table appear in package.json dependencies, project builds cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Install additional shadcn/ui primitives</name>
  <files>
    src/components/ui/select.tsx
    src/components/ui/tabs.tsx
    src/components/ui/popover.tsx
    src/components/ui/scroll-area.tsx
    src/components/ui/progress.tsx
    src/components/ui/checkbox.tsx
  </files>
  <action>
    Install 6 shadcn/ui components needed by Phase 2 features:

    Run these commands sequentially:
    ```
    npx shadcn@latest add select --yes --overwrite
    npx shadcn@latest add tabs --yes --overwrite
    npx shadcn@latest add popover --yes --overwrite
    npx shadcn@latest add scroll-area --yes --overwrite
    npx shadcn@latest add progress --yes --overwrite
    npx shadcn@latest add checkbox --yes --overwrite
    ```

    These are needed for:
    - **select**: Category/status filter dropdowns (COMP-04, AUDT-04)
    - **tabs**: View switching on audit plans page (calendar vs list view)
    - **popover**: Calendar date picker, filter popovers
    - **scroll-area**: Scrollable lists in dashboard widgets and detail modals
    - **progress**: Audit engagement progress bars (AUDT-05) — replaces manual div-based bars
    - **checkbox**: Multi-select filters in compliance registry

    After installation, verify each component file exists in src/components/ui/.
  </action>
  <verify>
    - All 6 files exist in src/components/ui/: select.tsx, tabs.tsx, popover.tsx, scroll-area.tsx, progress.tsx, checkbox.tsx
    - `pnpm build` completes without errors
  </verify>
  <done>6 new shadcn/ui components installed and importable, project builds cleanly</done>
</task>

</tasks>

<verification>
- `pnpm ls recharts @tanstack/react-table` shows both packages
- All shadcn/ui components in src/components/ui/ total at least 20 files (14 existing + 6 new)
- `pnpm build` passes
</verification>

<success_criteria>
- recharts and @tanstack/react-table in package.json dependencies
- 6 new shadcn/ui component files created
- Project builds without errors
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-screens/02-01-SUMMARY.md`
</output>
