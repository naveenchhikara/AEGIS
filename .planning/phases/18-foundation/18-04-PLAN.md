---
phase: 18-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/permissions.ts
  - src/lib/__tests__/permissions.test.ts
  - src/app/(onboarding)/onboarding/_components/step-5-user-invites.tsx
  - src/components/concurrent-audit/escalation-panel.tsx
  - src/components/concurrent-audit/irregularity-escalation-dialog.tsx
autonomous: true
requirements:
  - TERM-01

must_haves:
  truths:
    - "Every user-visible string that previously showed 'Chief Audit Executive' or 'CAE' (as a label) now displays 'Head of Internal Audit (HIA)'"
    - "The Role.CAE enum value is unchanged in the database and in code-level role checks"
    - "Permission names (cae:*) are unchanged"
    - "i18n files do not need changes (verified: no CAE display strings in message files)"
    - "Existing permissions test passes with updated expected value"
  artifacts:
    - path: "src/lib/permissions.ts"
      provides: "Central role display name mapping with HIA label"
      contains: "Head of Internal Audit"
    - path: "src/lib/__tests__/permissions.test.ts"
      provides: "Updated test assertion for HIA display name"
      contains: "Head of Internal Audit"
    - path: "src/app/(onboarding)/onboarding/_components/step-5-user-invites.tsx"
      provides: "Onboarding wizard with HIA label"
      contains: "Head of Internal Audit"
    - path: "src/components/concurrent-audit/escalation-panel.tsx"
      provides: "Escalation panel with HIA label"
      contains: "Head of Internal Audit"
    - path: "src/components/concurrent-audit/irregularity-escalation-dialog.tsx"
      provides: "Escalation dialog with HIA label"
      contains: "Head of Internal Audit"
  key_links:
    - from: "src/lib/permissions.ts"
      to: "all components"
      via: "getRoleDisplayName() function"
      pattern: "getRoleDisplayName"
---

<objective>
Rename all user-visible "Chief Audit Executive (CAE)" display strings to "Head of Internal Audit (HIA)" across the codebase while preserving the Role.CAE enum value and permission names.

Purpose: TERM-01 aligns the platform's terminology with current Indian audit standards. The rename is display-only — the underlying Role.CAE enum, permission names (cae:\*), and variable names remain unchanged to avoid breaking changes in the auth/permission system.

Output: 5 files updated with new display strings, verified by grep confirmation that no "Chief Audit Executive" display strings remain in non-generated source code.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-foundation/18-RESEARCH.md (Pattern 5: TERM-01 Terminology Rename — full file list and changes)
@src/lib/permissions.ts (line 467 — central fix)
@src/lib/__tests__/permissions.test.ts (lines 222-223 — test assertion)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all CAE display strings to HIA</name>
  <files>
    src/lib/permissions.ts
    src/lib/__tests__/permissions.test.ts
    src/app/(onboarding)/onboarding/_components/step-5-user-invites.tsx
    src/components/concurrent-audit/escalation-panel.tsx
    src/components/concurrent-audit/irregularity-escalation-dialog.tsx
  </files>
  <action>
Make these exact string replacements:

**1. `src/lib/permissions.ts` (line 467)**

```
- CAE: "Chief Audit Executive",
+ CAE: "Head of Internal Audit (HIA)",
```

**2. `src/lib/__tests__/permissions.test.ts` (lines 222-223)**

```
- it("returns 'Chief Audit Executive' for CAE", () => {
-   expect(getRoleDisplayName(Role.CAE)).toBe("Chief Audit Executive");
+ it("returns 'Head of Internal Audit (HIA)' for CAE", () => {
+   expect(getRoleDisplayName(Role.CAE)).toBe("Head of Internal Audit (HIA)");
```

**3. `src/app/(onboarding)/onboarding/_components/step-5-user-invites.tsx` (line 57)**

```
- CAE: "Chief Audit Executive",
+ CAE: "Head of Internal Audit (HIA)",
```

**4. `src/app/(onboarding)/onboarding/_components/step-5-user-invites.tsx` (line 216)**

```
- result.push("No Chief Audit Executive (CAE) invited");
+ result.push("No Head of Internal Audit (HIA) invited");
```

**5. `src/components/concurrent-audit/escalation-panel.tsx` (line 199)**

```
- the Chief Audit Executive, CEO, or Audit Committee of the Board as
+ the Head of Internal Audit (HIA), CEO, or Audit Committee of the Board as
```

**6. `src/components/concurrent-audit/irregularity-escalation-dialog.tsx` (line 56)**

```
- { value: "CAE", label: "Chief Audit Executive (CAE)" },
+ { value: "CAE", label: "Head of Internal Audit (HIA)" },
```

After making all changes, run verification grep:

```bash
grep -rn "Chief Audit Executive" src/ --include="*.tsx" --include="*.ts" | grep -v "generated/prisma" | grep -v "node_modules"
```

This must return ZERO results. Any remaining hits indicate a missed rename.

Also verify Role.CAE and cae: permission names are NOT changed:

```bash
grep -rn '"CAE"' src/lib/permissions.ts | head -5
```

Should still show role key and permission references using "CAE" string.
</action>
<verify>

1. `grep -rn "Chief Audit Executive" src/ --include="*.tsx" --include="*.ts" | grep -v generated | grep -v node_modules` returns zero results
2. `grep -rn "Head of Internal Audit" src/ --include="*.tsx" --include="*.ts"` returns 6 hits (the 5 files + test)
3. `pnpm vitest run src/lib/__tests__/permissions.test.ts` — all tests pass (including updated assertion)
4. `pnpm tsc --noEmit` — no TypeScript errors
   </verify>
   <done>All 5 source files + 1 test file updated. Zero "Chief Audit Executive" display strings remain. Role.CAE enum and permission names preserved. Tests pass.</done>
   </task>

</tasks>

<verification>
1. `grep -rn "Chief Audit Executive" src/ --include="*.tsx" --include="*.ts" | grep -v generated` — zero results
2. `grep -rn "Head of Internal Audit" src/ --include="*.tsx" --include="*.ts"` — 6 results across 5 files
3. `pnpm vitest run src/lib/__tests__/permissions.test.ts` — all tests pass
4. Verify Role.CAE still exists in generated Prisma types (not removed)
5. Verify "cae:" permission strings are unchanged in permissions.ts
</verification>

<success_criteria>

- Zero instances of "Chief Audit Executive" in non-generated TypeScript/TSX source files
- "Head of Internal Audit (HIA)" appears in permissions.ts, step-5-user-invites.tsx, escalation-panel.tsx, irregularity-escalation-dialog.tsx
- permissions.test.ts assertion updated and passing
- Role.CAE enum preserved, cae:\* permission names preserved
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/18-foundation/18-04-SUMMARY.md`
</output>
