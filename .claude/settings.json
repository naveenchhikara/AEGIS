{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r \".tool_input.file_path // empty\"); case \"$f\" in *.ts|*.tsx) node_modules/.bin/prettier --write \"$f\" >/dev/null 2>&1; node_modules/.bin/tsc --noEmit --pretty 2>&1 | head -20;; esac"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(tailscale status*)",
      "Bash(npx tsc --noEmit*)",
      "Bash(openssl s_client*)",
      "Bash(openssl x509 -noout*)"
    ]
  }
}
