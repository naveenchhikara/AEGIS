---
phase: 22-findings-and-meetings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/rbia/meeting-form.tsx
  - src/components/rbia/meeting-view.tsx
autonomous: true
requirements:
  - BMRP-02

must_haves:
  truths:
    - "Auditor can record an opening meeting with attendees selected from a multi-select dropdown, structured minutes, and checkbox sign-off"
    - "Auditor can record an exit meeting with the same form structure — exit section shows 'Not yet available' until engagement reaches EXIT_MEETING stage"
    - "Both opening and exit meetings appear on the same Meetings tab in two sections/cards"
    - "Meeting form requires at least 1 attendee — validation error prevents save without attendees"
    - "After save, the meeting displays in read-only summary view with an Edit button"
    - "Disabled transition button shows tooltip explaining prerequisite (e.g., 'Record opening meeting first')"
  artifacts:
    - path: "src/components/rbia/meeting-form.tsx"
      provides: "Meeting recording form with attendee multi-select, structured minutes template, and sign-off checkbox"
      min_lines: 80
    - path: "src/components/rbia/meeting-view.tsx"
      provides: "Read-only meeting summary view with Edit button and sign-off status"
      min_lines: 40
  key_links:
    - from: "src/components/rbia/meeting-form.tsx"
      to: "src/actions/rbia/meetings.ts"
      via: "calls recordMeeting and signOffMeeting server actions"
      pattern: "recordMeeting|signOffMeeting"
    - from: "src/components/rbia/meeting-form.tsx"
      to: "src/data-access/rbia-meetings.ts"
      via: "consumes MeetingAttendee, EngagementMeetingData types"
      pattern: "MeetingAttendee|EngagementMeetingData"
---

<objective>
Build the meeting recording forms for opening and exit meetings within the RBIA engagement workflow. Per user decisions, both meetings appear on the same Meetings tab as two sections, with structured templates for minutes, multi-select attendee dropdown, and checkbox sign-off.

Purpose: Provides the UI for ENGG-03 and ENGG-04 (meeting prerequisites for status transitions) which were implemented as server actions in Phase 20. Also partially satisfies BMRP-02 by completing the engagement workflow that leads to AP issuance.

Output: Two client components — meeting-form.tsx (create/edit form) and meeting-view.tsx (read-only summary).
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-findings-and-meetings/22-CONTEXT.md

@src/data-access/rbia-meetings.ts (MeetingAttendee, UpsertMeetingInput, EngagementMeetingData types)
@src/actions/rbia/meetings.ts (recordMeeting, signOffMeeting — from Phase 20)
@src/components/ui/card.tsx (Card layout)
@src/components/ui/button.tsx (Button)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build meeting-form.tsx — attendee multi-select, structured minutes, sign-off</name>
  <files>src/components/rbia/meeting-form.tsx</files>
  <action>
Create `src/components/rbia/meeting-form.tsx` as a `"use client"` component.

**Props:**

```typescript
interface MeetingFormProps {
  engagementId: string;
  meetingType: "OPENING" | "EXIT";
  existingMeeting: EngagementMeetingData | null;
  teamMembers: Array<{ id: string; name: string; role: string }>; // Pre-populated from engagement team
  branchStaff?: Array<{ name: string; designation: string }>; // Branch contacts for attendee selection
  onCancel: () => void;
  onSuccess: () => void;
}
```

**Implementation:**

1. Use `react-hook-form` with a Zod schema for validation. Schema must require:
   - `meetingDate` (required date)
   - `attendees` (required, min length 1 — per locked decision: at least 1 attendee required)
   - `minutesText` (optional but recommended — structured template)
   - `keyDiscussionPoints` (optional)

2. **Attendee selection** — Per locked decision: "Multi-select dropdown pre-populated with engagement team members and branch staff contacts, with option to add external names":
   - Show a multi-select dropdown populated with `teamMembers` and `branchStaff` props
   - Each attendee shows name + role/designation
   - "Add external attendee" row at bottom opens inline mini-form: name (required), role (optional text), designation (optional text)
   - Selected attendees shown as removable chips/badges below the dropdown
   - Validation: at least 1 attendee required — show error message if empty on submit

3. **Structured minutes template** — Per locked decision: "agenda items, decisions taken, action items, next steps":
   - A single rich textarea (or 4 labeled sections) with template headings:

     ```
     ## Agenda Items

     ## Decisions Taken

     ## Action Items

     ## Next Steps
     ```

   - Pre-fill the textarea with this template on first render (if no existing data)
   - If editing existing meeting, load the saved minutesText

4. **Key discussion points** — Optional textarea for exit meetings (per ENGG-04: "key discussion points").

5. **Checkbox sign-off** — Per locked decision: "Checkbox confirmation sign-off by the auditor recording the meeting":
   - Checkbox labeled "I confirm these meeting minutes are accurate"
   - Not part of the form submission itself — sign-off is a separate action (signOffMeeting from Phase 20)
   - Show the checkbox AFTER the meeting is saved, in the view mode

6. **Submit handler:**
   - Calls `recordMeeting` server action with the form data
   - Uses `useTransition` for pending state
   - On success: toast "Meeting recorded" + call `onSuccess()`
   - On error: show error message from server action response

7. **Edit mode:** When `existingMeeting` is provided, pre-fill all fields. Submit calls the same `recordMeeting` (which uses upsert internally).

8. **Date picker:** Use a simple HTML date input or shadcn DatePicker if available. Pre-fill with today's date for new meetings.
   </action>
   <verify>
   <automated>cd /Users/admin/Developer/AEGIS && npx tsc --noEmit --pretty 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
   <manual>Meeting form renders with attendee multi-select, structured minutes template, and date picker</manual>
   </verify>
   <done>Meeting form component with attendee multi-select (pre-populated from team members and branch staff, with add-external option), structured minutes template (agenda, decisions, action items, next steps), key discussion points textarea, and date picker. Validation requires at least 1 attendee. Calls recordMeeting server action on submit.</done>
   </task>

<task type="auto">
  <name>Task 2: Build meeting-view.tsx — read-only summary with sign-off action</name>
  <files>src/components/rbia/meeting-view.tsx</files>
  <action>
Create `src/components/rbia/meeting-view.tsx` as a `"use client"` component.

**Props:**

```typescript
interface MeetingViewProps {
  meeting: EngagementMeetingData;
  engagementId: string;
  canEdit: boolean; // Permission to edit/sign-off
}
```

**Implementation:**

1. **Layout:** A Card component displaying the saved meeting record as a read-only summary:
   - **Header:** Meeting type label ("Opening Meeting" / "Exit Meeting") + date formatted in Indian locale (use `formatDate` from `@/lib/utils`)
   - **Attendees section:** List of attendees as a compact table or tag list (name — role — designation)
   - **Minutes section:** Render `minutesText` as formatted text (preserve line breaks, heading structure)
   - **Key discussion points:** Show only if non-null
   - **Sign-off status:**
     - If `signedOff === true`: Green "Signed off" badge with date and signer name
     - If `signedOff === false` and `canEdit`: Show checkbox "I confirm these meeting minutes are accurate" + "Sign Off" button
     - Clicking "Sign Off" calls `signOffMeeting` server action (from Phase 20)
     - Use `useTransition` for pending state on sign-off
   - **Edit button:** Only shown if `canEdit` and `signedOff === false` — triggers callback to switch to edit mode

2. **Tooltip on disabled state** — Per locked decision: When meeting is signed off, the Edit button is disabled with tooltip "Meeting already signed off — cannot edit".

3. **Sign-off flow:**
   - Calls `signOffMeeting({ engagementId, meetingType: meeting.meetingType })`
   - On success: toast "Meeting signed off" + `router.refresh()`
   - On error: show error message

4. Per discretion decision: "Meeting view mode after save (read-only summary with Edit button)" — implementing as read-only view with Edit button (not always-editable).
   </action>
   <verify>
   <automated>cd /Users/admin/Developer/AEGIS && npx tsc --noEmit --pretty 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
   <manual>Meeting view shows attendee list, minutes, and sign-off status with Edit button</manual>
   </verify>
   <done>Meeting view component displays saved meeting as read-only Card with formatted attendees list, structured minutes, sign-off status badge, and conditional Edit/Sign-off buttons. Sign-off calls signOffMeeting server action. Edit only available when not signed off.</done>
   </task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles both components without errors
2. Meeting form validates at least 1 attendee before submission
3. Structured minutes template pre-fills on first render
4. Meeting view shows read-only summary with correct sign-off state
5. Sign-off action calls signOffMeeting and refreshes page
6. Edit button only shown when not signed off and user has permission
</verification>

<success_criteria>
Two working client components: meeting-form with attendee multi-select, structured minutes template, and validation; meeting-view with read-only summary and sign-off action. Both follow existing form patterns (react-hook-form + Zod) and UI conventions (shadcn Card, Badge, Button).
</success_criteria>

<output>
After completion, create `.planning/phases/22-findings-and-meetings/22-02-SUMMARY.md`
</output>
