---
phase: 23-bm-response-and-reporting
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/rbia/rbia-score-gauge.tsx
  - src/components/rbia/rbia-module-breakdown.tsx
  - src/components/rbia/rbia-score-trend.tsx
  - src/data-access/rbia-analytics.ts
autonomous: true
requirements:
  - REPT-02

must_haves:
  truths:
    - "Historical trend line chart shows composite score on Y-axis and engagements on X-axis with module-level lines toggleable via legend"
    - "Score gauge component (extending Phase 22 stub) renders composite RBIA score as circular donut with percentage and color-coded rating band"
    - "Module breakdown component (extending Phase 22 stub) shows grid of cards with accordion drill-down from module to sub-module to leaf item"
    - "Analytics DAL provides data functions for score visualization page and board analytics (Plan 23-05)"
    - "All drill-down happens on the same page without navigation or route changes"
  artifacts:
    - path: "src/components/rbia/rbia-score-gauge.tsx"
      provides: "Circular gauge using RadialBarChart with percentage and rating band"
      contains: "RadialBarChart"
    - path: "src/components/rbia/rbia-module-breakdown.tsx"
      provides: "Module grid with accordion drill-down into sub-modules and leaf items"
      min_lines: 100
    - path: "src/components/rbia/rbia-score-trend.tsx"
      provides: "Historical trend line chart from getBranchScoreHistory data"
      contains: "LineChart"
    - path: "src/data-access/rbia-analytics.ts"
      provides: "DAL functions for score visualization and analytics data"
      contains: "server-only"
  key_links:
    - from: "src/components/rbia/rbia-score-gauge.tsx"
      to: "src/components/ui/chart.tsx"
      via: "ChartContainer wrapper for RadialBarChart"
      pattern: "ChartContainer"
    - from: "src/components/rbia/rbia-score-trend.tsx"
      to: "src/data-access/rbia-scoring.ts"
      via: "getBranchScoreHistory provides historical frozen scores"
      pattern: "getBranchScoreHistory"
    - from: "src/components/rbia/rbia-module-breakdown.tsx"
      to: "BranchRbiaScore.scoringTreeSnapshot"
      via: "JSONB snapshot parsed server-side and passed as prop for drill-down"
      pattern: "scoringTreeSnapshot"
---

<objective>
Complete the RBIA score visualization by extending Phase 22 stubs (REPT-01 gauge and REPT-03 drill-down) and adding the historical trend line chart (REPT-02). Create the analytics DAL functions needed by this plan and Plan 23-05.

Purpose: Phase 22 delivers REPT-01 (composite score gauge + module breakdown) and REPT-03 (drill-down) as stubs. This plan completes those stubs with full data wiring and adds REPT-02 (historical trend) which is Phase 23's own requirement. The analytics DAL serves both the score display page and board analytics.

Note: REPT-01 and REPT-03 are assigned to Phase 22 in REQUIREMENTS.md traceability. This plan extends their Phase 22 stubs — it does NOT rebuild them from scratch. If Phase 22 already delivers fully working gauge and drill-down components, adapt this plan to only add the trend chart and DAL.

Output: Extended/completed score components (`rbia-score-gauge.tsx`, `rbia-module-breakdown.tsx`), new trend chart (`rbia-score-trend.tsx`), and analytics DAL (`rbia-analytics.ts`).
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-bm-response-and-reporting/23-RESEARCH.md

@src/components/dashboard/widgets/daksh-score-gauge.tsx (RadialBarChart gauge pattern)
@src/components/dashboard/widgets/health-score-gauge.tsx (RadialBarChart with PolarAngleAxis)
@src/components/ui/chart.tsx (ChartContainer, ChartConfig)
@src/data-access/rbia-scoring.ts (getBranchScoreHistory, getEngagementBranchScore)
@src/lib/rbia-scoring-engine.ts (getRatingBand, rating band thresholds)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete RBIA score gauge (extend Phase 22 stub) + create analytics DAL</name>
  <files>src/components/rbia/rbia-score-gauge.tsx, src/data-access/rbia-analytics.ts</files>
  <action>
**Pre-check:** Inspect `src/components/rbia/rbia-score-gauge.tsx` — Phase 22 delivers REPT-01 as a stub. If the gauge is already fully functional (RadialBarChart, color coding, center overlay), skip the gauge work and only create the DAL. If it's a placeholder/stub, complete it with the implementation below.

**Analytics DAL** — Create `src/data-access/rbia-analytics.ts`:

1. `import "server-only"` at top
2. Import `prismaForTenant` from `@/data-access/prisma`, `AuthSession` from `@/lib/auth`

Export `getScoreDisplayData(session: AuthSession, engagementId: string)`:

- Extract tenantId from session
- Query `branchRbiaScore.findUnique` where `{ engagementId }` with tenant check
- If found: return `{ compositeScore: Number(score.compositeScore), ratingBand: score.ratingBand, moduleScores: score.moduleScores as Record<string, number>, scoringTreeSnapshot: score.scoringTreeSnapshot, frozenAt: score.frozenAt }`
- If not found: return `null`

Export `getRbiaAnalyticsSummary(session: AuthSession)`:

- For board analytics (used in Plan 23-05)
- Query all frozen `BranchRbiaScore` records for tenant: `findMany` where `{ tenantId, frozenAt: { not: null } }`, include `engagement.branch.name`
- Group by ratingBand: compute count per band (VERY_GOOD, GOOD, SATISFACTORY, MODERATE, POOR)
- Compute average module scores across all branches (aggregate moduleScores JSONB)
- Return `{ totalAudited, averageComposite, ratingDistribution, moduleAverages, scores[] }`

Export `getRbiaAnalyticsByPeriod(session: AuthSession, startDate: Date, endDate: Date)`:

- Same as getRbiaAnalyticsSummary but filtered by `frozenAt` date range
- For period selector in board analytics

**Score Gauge** — Create `src/components/rbia/rbia-score-gauge.tsx`:

"use client" component following the existing `DakshScoreGauge` / `HealthScoreGauge` pattern.

Props: `compositeScore: number` (0.0-1.0), `ratingBand: string`.

Rating band color map per CONTEXT.md locked decision:

```typescript
const RBIA_RATING_COLORS: Record<string, string> = {
  VERY_GOOD: "hsl(142 60% 35%)", // dark green
  GOOD: "hsl(213 90% 55%)", // blue
  SATISFACTORY: "hsl(45 96% 56%)", // yellow
  MODERATE: "hsl(25 95% 53%)", // orange
  POOR: "hsl(0 84% 60%)", // red
};
```

Implementation:

- `const percentage = Math.round(compositeScore * 100)`
- `const color = RBIA_RATING_COLORS[ratingBand] ?? RBIA_RATING_COLORS.POOR`
- Use `ChartContainer` + `RadialBarChart` from recharts with:
  - `startAngle={90}` `endAngle={-270}` for clockwise fill from top
  - `innerRadius="70%"` `outerRadius="100%"` for donut effect
  - `PolarAngleAxis type="number" domain={[0, 100]} tick={false}`
  - `RadialBar dataKey="value" cornerRadius={10} background`
- Center overlay (absolute positioned, `pointer-events-none` per CLAUDE.md gotcha):
  - Large percentage text: `text-5xl font-bold`
  - Rating band label below: `text-sm font-medium` with color matching the band
  - Format rating band: replace underscores with spaces (`ratingBand.replace(/_/g, " ")`)
- Size: `min-h-[200px] w-full max-w-[200px] mx-auto`

Per CONTEXT.md: "Composite RBIA score displayed as a large circular gauge (donut/arc) — shows percentage with rating band label and color, similar to credit score displays"
</action>
<verify>
<automated>cd /Users/admin/Developer/AEGIS && npx tsc --noEmit 2>&1 | grep -E "(rbia-score-gauge|rbia-analytics)" | head -20</automated>
<manual>Verify gauge renders with correct color coding and center overlay text</manual>
</verify>
<done>Score gauge component renders composite RBIA score as a circular donut chart with percentage and rating band label. Analytics DAL provides data for score display page and board analytics.</done>
</task>

<task type="auto">
  <name>Task 2: Complete module breakdown drill-down (extend Phase 22 stub) + create historical trend chart</name>
  <files>src/components/rbia/rbia-module-breakdown.tsx, src/components/rbia/rbia-score-trend.tsx</files>
  <action>
**Pre-check:** Inspect `src/components/rbia/rbia-module-breakdown.tsx` — Phase 22 delivers REPT-03 drill-down as a stub. If the module breakdown already has accordion expansion with recursive tree rendering, skip to the trend chart. If it's a placeholder/stub, complete it with the implementation below.

**Module Breakdown** — Extend `src/components/rbia/rbia-module-breakdown.tsx`:

"use client" component.

Props:

- `moduleScores: Record<string, number>` — module code → score (0.0-1.0)
- `scoringTreeSnapshot: ScoreTreeNode[] | null` — full tree from BranchRbiaScore JSONB

Define local type:

```typescript
type ScoreTreeNode = {
  nodeId: string;
  code: string;
  name?: string;
  weight: number;
  isCritical: boolean;
  isLeaf: boolean;
  scoreLabel: string | null;
  children: ScoreTreeNode[];
};
```

Rating band helper: compute rating band from score decimal using same thresholds as scoring engine (>0.8 Very Good, >0.65 Good, >0.5 Satisfactory, >0.4 Moderate, <=0.4 Poor).

Module grid layout:

- CSS grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
- Each module card: `<Card>` with module code/name header, score percentage text, colored `<Progress>` bar (color from rating band), rating label badge
- Use shadcn/ui `<Accordion>` (AccordionItem, AccordionTrigger, AccordionContent) or simple `useState` toggle per card
- On card click/trigger: expand to show sub-modules as nested rows

Drill-down levels (per CONTEXT.md "accordion-style, all drill-down on same page"):

1. Module card (depth 1) — shows module name + score
2. Expand → sub-module rows (depth 2-3) — indented, each with name + score
3. Expand sub-module → leaf items (depth 4-5) — indented further, each with scoreLabel badge (FULLY/LARGELY/PARTIALLY/NON_COMPLIANT), isCritical flag indicator

Recursive renderer function:

```typescript
function renderTreeNode(node: ScoreTreeNode, depth: number) {
  // Render node row with indentation based on depth
  // If node has children, render as accordion trigger
  // Score label badges: green (FULLY), blue (LARGELY), yellow (PARTIALLY), red (NON_COMPLIANT)
  // Critical items: red border or exclamation icon
}
```

If `scoringTreeSnapshot` is null (not yet frozen), show module scores from `moduleScores` prop without drill-down, with "Freeze score to enable drill-down" message.

**Score Trend** — Create `src/components/rbia/rbia-score-trend.tsx`:

"use client" component.

Props: `scores: Array<{ frozenAt: string | Date, compositeScore: number, ratingBand: string, moduleScores: Record<string, number> }>` — from `getBranchScoreHistory()` DAL function.

Implementation:

- Use `ChartContainer` + recharts `LineChart` with:
  - `XAxis dataKey="date"` (format frozenAt to `en-IN` short date via `formatDate` from `@/lib/utils`)
  - `YAxis domain={[0, 100]}` with percentage label
  - `CartesianGrid strokeDasharray="3 3"`
  - Main line: `<Line type="monotone" dataKey="composite" stroke="hsl(213 90% 55%)" strokeWidth={2} dot />`
  - Module lines (optional, toggleable via legend): one `<Line>` per module key, different stroke colors
  - `<Tooltip>` with composite score and rating band
  - `<Legend>` for toggling module lines

Per CONTEXT.md: "Historical trend as a line chart — composite score on Y-axis, engagements on X-axis (labeled by date), with optional module-level lines toggled via legend"

Data transformation:

```typescript
const chartData = scores.map((s) => ({
  date: formatDate(s.frozenAt),
  composite: Math.round(Number(s.compositeScore) * 100),
  ...Object.fromEntries(
    Object.entries(s.moduleScores as Record<string, number>).map(
      ([code, score]) => [code, Math.round(score * 100)],
    ),
  ),
}));
```

If scores array has < 2 entries, show "Insufficient data for trend" message instead of chart.

Import all icons from `@/lib/icons`.
</action>
<verify>
<automated>cd /Users/admin/Developer/AEGIS && npx tsc --noEmit 2>&1 | grep -E "(rbia-module-breakdown|rbia-score-trend)" | head -20</automated>
<manual>Verify accordion drill-down expands modules in place, trend chart renders with toggleable lines</manual>
</verify>
<done>Module breakdown component displays grid of module cards with accordion drill-down from module → sub-module → leaf item level. Trend chart shows historical composite score with optional module-level lines toggled via legend. All drill-down is in-page with no navigation.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all new files
2. Score gauge uses RadialBarChart with correct startAngle/endAngle for clockwise fill
3. Center overlay has `pointer-events-none` to prevent chart tooltip blocking
4. Rating band colors match CONTEXT.md: Poor=red, Moderate=orange, Satisfactory=yellow, Good=blue, Very Good=dark green
5. Module breakdown uses accordion expansion — no route changes on click
6. ScoreTreeNode type matches the shape from Phase 20 freezeRbiaScore (nodeId, code, weight, isCritical, isLeaf, scoreLabel, children)
7. Trend chart uses LineChart with composite + module lines and Legend toggle
8. Icons imported from `@/lib/icons`
</verification>

<success_criteria>
Score visualization shows composite RBIA score as a circular gauge with color-coded rating band, module breakdown as expandable cards with full drill-down to leaf item level, and historical trend as a line chart with module toggles. All interaction is in-page without navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/23-bm-response-and-reporting/23-03-SUMMARY.md`
</output>
