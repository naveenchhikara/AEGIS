---
phase: 01-project-setup
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - components.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true

must_haves:
  truths:
    - Next.js 14 project runs locally with dev server
    - shadcn/ui is initialized and configured
    - TypeScript and Tailwind CSS are working
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next, react, typescript, tailwindcss, eslint, prettier"
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "component paths, tailwind config, rsc config"
    - path: "src/app/layout.tsx"
      provides: "Root layout"
      contains: "html, body, font setup"
  key_links:
    - from: "shell"
      to: "localhost:3000"
      via: "pnpm dev"
      pattern: "pnpm dev"
---

<objective>
Initialize Next.js 14 project with TypeScript, Tailwind CSS, and shadcn/ui component library.

Purpose: Set up the foundational project infrastructure with App Router, type safety, utility-first styling, and accessible pre-built components.

Output: Working Next.js project with shadcn/ui configured
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14 with App Router</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/globals.css</files>
  <action>
    Initialize Next.js project in current directory:

    1. Run: pnpm create next-app@latest . --typescript --tailwind --eslint --app --src-dir --turbo --yes
       - Creates App Router with /src/app
       - TypeScript configuration
       - Tailwind CSS with default theme
       - ESLint with Next.js config
       - Turbopack bundler

    2. Verify package.json contains: next, react, react-dom, typescript, tailwindcss, eslint, eslint-config-next

    3. Add Prettier for code quality:
       pnpm add -D prettier prettier-plugin-tailwindcss

    4. Create .prettierrc with:
       {
         "semi": true,
         "singleQuote": false,
         "plugins": ["prettier-plugin-tailwindcss"]
       }

    5. Verify project runs: pnpm dev (should start on localhost:3000)
  </action>
  <verify>pnpm dev starts successfully on localhost:3000 showing default Next.js page</verify>
  <done>Next.js project initialized with TypeScript, Tailwind, ESLint, Prettier</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui</name>
  <files>components.json, tailwind.config.ts, src/app/globals.css</files>
  <action>
    Initialize shadcn/ui component library:

    1. Run: pnpm dlx shadcn@latest init --yes --defaults
       - Creates components.json configuration
       - Updates tailwind.config.ts with shadcn paths
       - Adds CSS variables to globals.css
       - Creates src/components/ui directory

    2. Verify components.json exists with:
       - "$schema" pointing to shadcn schema
       - "style" set to "default"
       - "tailwind.css" pointing to src/app/globals.css
       - "aliases" using @/ prefix

    3. Add required utils if not created: src/lib/utils.ts with cn() function

    4. Do NOT add components yet - that's in next plan
  </action>
  <verify>components.json exists and tailwind.config.ts references shadcn paths</verify>
  <done>shadcn/ui initialized and ready for component installation</done>
</task>

<task type="auto">
  <name>Task 3: Configure path aliases and type imports</name>
  <files>tsconfig.json</files>
  <action>
    Verify tsconfig.json has correct path aliases:

    1. Check that compilerOptions.paths includes:
       "@/*": ["./*"]

    2. If src-dir was used, ensure it points to ./src/* not ./*

    3. Create test file to verify imports work:
       Create src/test-import.ts with:
       import { cn } from '@/lib/utils'
       console.log('Import test:', cn)

    4. Delete test file after verification

    5. Verify no TypeScript errors in existing files
  </action>
  <verify>tsconfig.json has "@/*": ["./*"] or ["./src/*"] and imports resolve correctly</verify>
  <done>Path aliases configured for clean imports from @/ prefix</done>
</task>

</tasks>

<verification>
- pnpm dev starts without errors
- Accessing localhost:3000 shows Next.js default page
- components.json exists with shadcn configuration
- TypeScript compiles without errors
- Tailwind classes work (check globals.css)
</verification>

<success_criteria>
- Next.js 14 project runs locally at localhost:3000
- shadcn/ui is initialized (components.json exists)
- TypeScript and Tailwind CSS configured
- Path aliases (@/) work for imports
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md`
</output>
