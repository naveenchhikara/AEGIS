---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - Project Doc/rbi-circulars/index.md
  - Project Doc/common-rbi-observations.md
autonomous: true

must_haves:
  truths:
    - TypeScript types define all data structures needed for demo data
    - RBI circulars index documents mapping circulars to compliance requirements
    - Common observations document provides finding templates
  artifacts:
    - path: "src/types/index.ts"
      provides: "TypeScript interfaces for all domain models"
      exports: ["BankProfile", "ComplianceRequirement", "AuditPlan", "Finding", "ComplianceCalendar"]
    - path: "Project Doc/rbi-circulars/index.md"
      provides: "RBI circulars catalog and requirement mapping"
      contains: "circular ID, title, requirement mapping"
    - path: "Project Doc/common-rbi-observations.md"
      provides: "Common RBI observation patterns for finding generation"
      contains: "observation categories, root causes, risk impacts"
  key_links:
    - from: "src/data/*.ts"
      to: "src/types/index.ts"
      via: "import types"
      pattern: "import.*from '@/types'"
---

<objective>
Create foundational data structures and documentation for the AEGIS platform.

Purpose: Define TypeScript interfaces for all domain models (bank profile, compliance, audits, findings) and document RBI circulars with common observation patterns. This provides the data architecture that all components will consume.

Output: Type definitions file, RBI circulars index, and common observations document
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript type definitions</name>
  <files>src/types/index.ts</files>
  <action>
    Create src/types/index.ts with all domain interfaces:

    1. BankProfile interface with:
       - id, name, shortName, established (ISO date)
       - branches: Branch[] (id, name, code, location, manager)
       - departments: Department[] (id, name, head)
       - staff: StaffMember[] (id, name, email, role, department)

    2. ComplianceRequirement interface with:
       - id, categoryId, title, description, reference (RBI circular)
       - status: 'compliant' | 'partial' | 'non-compliant' | 'pending'
       - dueDate (ISO date), evidenceCount, assignedTo, createdAt (ISO date)

    3. AuditPlan interface with:
       - id, name, type (branch-audit, is-audit, credit-audit, compliance-audit, revenue-audit)
       - branchId?, departmentId?, plannedStartDate, plannedEndDate (ISO dates)
       - assignedTeam: string[], status, progress (0-100), createdAt (ISO date)

    4. Finding interface with:
       - id, auditId, title, category, severity, status
       - observation, rootCause, riskImpact, auditeeResponse, actionPlan
       - assignedAuditor, createdAt, updatedAt (ISO dates)
       - timeline: TimelineEntry[] (id, date, action, actor)

    5. ComplianceCalendar interface with:
       - id, requirementId, title, dueDate (ISO date)
       - status: 'upcoming' | 'due-soon' | 'overdue'
       - priority: 'high' | 'medium' | 'low'

    Use exact type names and property names from RESEARCH.md. Export all interfaces.
  </action>
  <verify>File exists at src/types/index.ts with all 5 interfaces defined and exported</verify>
  <done>All domain types defined with correct property names and union types</done>
</task>

<task type="auto">
  <name>Task 2: Create RBI circulars index document</name>
  <files>Project Doc/rbi-circulars/index.md</files>
  <action>
    Create Project Doc/rbi-circulars/ directory if not exists.

    Create index.md documenting:
    1. Directory structure for storing RBI circular PDFs
    2. Index table mapping circulars to compliance requirements:
       - Circular ID, Title, Date, Category, Mapped Requirements
    3. Categories: Risk Management, Governance, Operations, IT, Credit, Market Risk
    4. Note that this is a catalog - actual PDFs will be added later

    Include sample entries showing the mapping format.
  </action>
  <verify>File exists at Project Doc/rbi-circulars/index.md with circular catalog structure</verify>
  <done>RBI circulars catalog structure documented for reference</done>
</task>

<task type="auto">
  <name>Task 3: Create common RBI observations document</name>
  <files>Project Doc/common-rbi-observations.md</files>
  <action>
    Create Project Doc/common-rbi-observations.md documenting:

    1. Observation categories (governance, operations, credit, IT, compliance)
    2. For each category, list 3-5 common RBI exam findings with:
       - Observation statement
       - Root cause pattern
       - Risk impact description
       - Typical action plan

    3. Include severity patterns (critical, high, medium, low)
    4. Reference real RBI inspection patterns where applicable

    These serve as templates for generating realistic findings in demo data.
  </action>
  <verify>File exists at Project Doc/common-rbi-observations.md with observation templates</verify>
  <done>Common RBI observation patterns documented for finding generation</done>
</task>

</tasks>

<verification>
- All TypeScript interfaces export correctly (no syntax errors)
- RBI circulars index has clear mapping structure
- Common observations document provides realistic finding templates
</verification>

<success_criteria>
- src/types/index.ts contains 5 interfaces: BankProfile, ComplianceRequirement, AuditPlan, Finding, ComplianceCalendar
- Project Doc/rbi-circulars/index.md documents circular catalog structure
- Project Doc/common-rbi-observations.md provides finding generation templates
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
