---
phase: 01-project-setup
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/card.tsx
  - src/components/ui/sidebar.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/lib/utils.ts
  - src/data
autonomous: true

must_haves:
  truths:
    - Base shadcn/ui components installed and working
    - Project directories created (components, lib, data, public)
    - Components can be imported from @/components/ui/*
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button component"
      exports: ["Button", "buttonVariants"]
    - path: "src/components/ui/sidebar.tsx"
      provides: "Sidebar navigation component"
      exports: ["Sidebar", "SidebarContent", "SidebarMenu"]
    - path: "src/data"
      provides: "Directory for JSON demo data files"
      contains: "empty directory ready for data files"
  key_links:
    - from: "src/components"
      to: "@/components/ui"
      via: "path alias"
      pattern: "import.*from '@/components/ui'"
---

<objective>
Install base shadcn/ui components and create project directory structure.

Purpose: Set up the foundational UI components needed for login screen and navigation, plus create the directory structure for the entire project (components, lib, data, public).

Output: Installed components and project directories ready for feature development
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install base shadcn/ui components</name>
  <files>src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/card.tsx, src/components/ui/sidebar.tsx, src/components/ui/dropdown-menu.tsx</files>
  <action>
    Install required shadcn/ui components:

    Run these commands sequentially:
    1. pnpm dlx shadcn@latest add button --yes
    2. pnpm dlx shadcn@latest add input --yes
    3. pnpm dlx shadcn@latest add label --yes
    4. pnpm dlx shadcn@latest add card --yes
    5. pnpm dlx shadcn@latest add sidebar --yes
    6. pnpm dlx shadcn@latest add dropdown-menu --yes

    Each command adds:
    - Component file to src/components/ui/
    - Any required dependencies (class-variance-authority, clsx, tailwind-merge)

    Verify all component files exist after installation.
  </action>
  <verify>All 6 component files exist in src/components/ui/ directory</verify>
  <done>Base UI components installed: button, input, label, card, sidebar, dropdown-menu</done>
</task>

<task type="auto">
  <name>Task 2: Create project directory structure</name>
  <files>src/data, src/lib</files>
  <action>
    Create directory structure if not exists:

    1. Verify/create src/data/ for JSON demo data files
    2. Verify/create src/lib/ for utility functions
    3. Verify/create public/ directory for static assets
    4. Verify/create src/components/ directory for custom components (separate from ui/)

    Create index files to prevent issues:
    - src/data/index.ts (empty, will export data later)
    - src/lib/index.ts (export utils if not already done)

    Run: mkdir -p src/data src/lib src/components public
  </action>
  <verify>Directories exist: src/data, src/lib, src/components, public</verify>
  <done>Project directory structure created for data, utilities, components, and assets</done>
</task>

<task type="auto">
  <name>Task 3: Verify utils and set up icons</name>
  <files>src/lib/utils.ts, package.json</files>
  <action>
    1. Verify src/lib/utils.ts exists with cn() function:
       import { clsx, type ClassValue } from "clsx"
       import { twMerge } from "tailwind-merge"
       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs))
       }

    2. Install lucide-react for icons (comes with shadcn but verify):
       pnpm add lucide-react

    3. Create src/lib/icons.ts as a re-export convenience file:
       export * from 'lucide-react'

    4. Verify package.json includes lucide-react in dependencies
  </action>
  <verify>src/lib/utils.ts has cn() function and lucide-react is in package.json</verify>
  <done>Utility functions configured and icon library installed</done>
</task>

</tasks>

<verification>
- All component files exist in src/components/ui/
- Components can be imported from @/components/ui/*
- Directories src/data, src/lib, src/components, public exist
- lucide-react icons are available for import
</verification>

<success_criteria>
- 6 shadcn/ui components installed: button, input, label, card, sidebar, dropdown-menu
- Project directories created: src/data, src/lib, src/components, public
- lucide-react icon library installed
- cn() utility function available
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-03-SUMMARY.md`
</output>
