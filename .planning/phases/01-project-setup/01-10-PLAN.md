---
phase: 01-project-setup
plan: 10
type: execute
wave: 7
depends_on: [01-01, 01-08, 01-09]
files_modified:
  - src/data/compliance-calendar.ts
  - src/data/index.ts
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: false

must_haves:
  truths:
    - Compliance calendar loads with upcoming deadlines
    - Dashboard displays bank name from loaded data
    - Dashboard shows stats from all loaded data (requirements count, audit count, findings count)
  artifacts:
    - path: "src/data/compliance-calendar.ts"
      provides: "Compliance calendar with deadlines"
      exports: ["complianceCalendar"]
      contains: "upcoming compliance deadlines"
    - path: "src/data/index.ts"
      provides: "Complete central data export index"
      exports: ["bankProfile", "complianceRequirements", "auditPlans", "findings", "complianceCalendar"]
      contains: "all data module re-exports"
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard with dynamic data display"
      contains: "bank name, stats from loaded data"
  key_links:
    - from: "src/data/compliance-calendar.ts"
      to: "src/data/compliance-requirements.ts"
      via: "requirementId references"
      pattern: "requirementId.*RBI-"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "@/data"
      via: "ES imports"
      pattern: "import.*from '@/data'"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/data/bank-profile.ts"
      via: "data import"
      pattern: "bankProfile.name"
---

<objective>
Create compliance calendar and connect demo data to dashboard.

Purpose: Generate compliance calendar with upcoming deadlines and wire all demo data to the dashboard page for visual verification that the complete data set loads correctly.

Output: Complete demo data set loaded and displayed on dashboard
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-setup/01-CONTEXT.md
@.planning/phases/01-project-setup/01-RESEARCH.md
@.planning/phases/01-project-setup/01-01-SUMMARY.md
@.planning/phases/01-project-setup/01-08-SUMMARY.md
@.planning/phases/01-project-setup/01-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compliance calendar data</name>
  <files>src/data/compliance-calendar.ts</files>
  <action>
    Create src/data/compliance-calendar.ts with upcoming deadlines:

    Import ComplianceCalendar type from @/types

    Create 15-20 calendar entries for upcoming compliance deadlines:
    - Monthly: ALM return, NPA reporting
    - Quarterly: Basel III disclosure, risk committee report
    - Half-yearly: ICAAP submission, stress testing
    - Annual: financial audit, board approval of risk policies

    Each entry needs:
    - id: "CAL-001" format
    - requirementId: Reference to compliance requirement
    - title: Descriptive deadline title
    - dueDate: Dates spread across Feb-Jun 2026
    - status: upcoming, due-soon (within 7 days), overdue
    - priority: high, medium, low

    Export: export const complianceCalendar: ComplianceCalendar[] = [ ... ]
  </action>
  <verify>File exists with 15-20 calendar entries with upcoming deadlines</verify>
  <done>Compliance calendar created with upcoming deadlines</done>
</task>

<task type="auto">
  <name>Task 2: Update data index</name>
  <files>src/data/index.ts</files>
  <action>
    Update src/data/index.ts to include compliance calendar:

    Add re-export:
    export * from './compliance-calendar'

    The file should now export all data modules: bankProfile, complianceRequirements, auditPlans, findings, complianceCalendar
  </action>
  <verify>File exports complianceCalendar in addition to existing modules</verify>
  <done>Data index complete with all modules</done>
</task>

<task type="auto">
  <name>Task 3: Connect data to dashboard</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
    Update src/app/(dashboard)/dashboard/page.tsx to display data:

    1. Import all data from @/data:
       import { bankProfile, complianceRequirements, auditPlans, findings } from '@/data'

    2. Replace static content with dynamic data:
       - Display bank name: {bankProfile.name}
       - Replace placeholder stats section with actual counts:
         * {complianceRequirements.length} Compliance Requirements
         * {auditPlans.length} Planned Audits
         * {findings.filter(f => f.status !== 'closed').length} Open Findings
       - Use Card components for each stat (already installed via 01-03)

    3. Add brief description:
       - Branches: {bankProfile.branches.length}
       - Departments: {bankProfile.departments.length}
       - Staff: {bankProfile.staff.length}

    This verifies the entire data pipeline works: types -> data files -> imports -> display.
  </action>
  <verify>Dashboard displays bank name and shows counts from loaded data</verify>
  <done>Dashboard connected to demo data and displays live statistics</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete demo data set loaded and displayed on dashboard</what-built>
  <how-to-verify>
    1. Start dev server: pnpm dev
    2. Visit http://localhost:3000/dashboard
    3. Verify:
       - Bank name "Sahyadri Urban Co-operative Bank Ltd" displays
       - Stats show: 50+ Compliance Requirements, 8-10 Planned Audits, 35 Findings
       - Branch count, department count, staff count display correctly
       - No console errors about data imports
    4. Check browser DevTools console to verify data loaded without errors
    5. (Optional) Verify data structure:
       - Add temporary console.log to dashboard: console.log({ bankProfile, complianceRequirements, auditPlans, findings })
       - Refresh and check browser console for data output
       - Remove console.log after verification
  </how-to-verify>
  <resume-signal>Type "approved" if data loads correctly and dashboard displays stats, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Compliance calendar file exists with 15-20 entries
- Data index exports all 5 modules
- Dashboard displays bank name from data
- Dashboard shows counts from all data sources
- No console errors
- Data relationships intact (calendar references requirements, findings reference audits)
</verification>

<success_criteria>
- Calendar: 15-20 upcoming compliance deadlines
- Dashboard displays: bank name, compliance count, audit count, findings count, branch count, department count, staff count
- All data imports without TypeScript errors
- Complete demo data pipeline verified working
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-10-SUMMARY.md`
</output>
