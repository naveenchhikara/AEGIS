---
phase: 02-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/audit-execution/page.tsx (created)
  - src/app/(dashboard)/admin/page.tsx (created)
  - src/data-access/audit-execution.ts (modified)
  - src/components/audit-execution/engagements-table.tsx (created)
  - src/lib/nav-items.ts (modified)
autonomous: true
---

<objective>
## Goal
Create the two missing index pages (/audit-execution and /admin) that currently return 404, and fix the sidebar admin link to point to the new admin hub.

## Purpose

These are P0 flow blockers — users cannot browse audit engagements or access an admin hub. Fixes ISS-001, ISS-003, and ISS-011 (sidebar fix).

## Output

- `/audit-execution` shows table of all AuditEngagements with status, branch, dates, team
- `/admin` shows hub page with cards linking to each admin section
- Sidebar Admin link updated to `/admin`
  </objective>

<context>
@.paul/PROJECT.md
@.paul/phases/01-e2e-audit-flow/ISSUES.md
@src/app/(dashboard)/findings/page.tsx (pattern reference for index page)
@src/app/(dashboard)/admin/users/page.tsx (pattern reference for admin page)
@src/data-access/audit-execution.ts (DAL — needs getEngagements list function)
@src/lib/nav-items.ts (sidebar — admin link fix)
@prisma/schema.prisma (AuditEngagement model)
</context>

<acceptance_criteria>

## AC-1: Audit Execution Index Page Works

Given a user with audit_execution:read permission
When they navigate to /audit-execution
Then they see a table of all engagements for their tenant with columns: Branch, Audit Area, Status, Start Date, End Date, Team Lead
And a "Create Engagement" button links to /audit-execution/create
And table rows are clickable → /audit-execution/{engagementId}

## AC-2: Admin Hub Page Works

Given a user with admin:manage_users permission
When they navigate to /admin
Then they see cards/links to Users, Branches, Zones, Templates, and RAM Config
And each card links to the correct /admin/{section} page

## AC-3: Sidebar Admin Link Fixed

Given the sidebar navigation
When Admin link is rendered
Then it points to /admin (not /admin/users)
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Create getEngagements DAL function and EngagementsTable component</name>
  <files>
    src/data-access/audit-execution.ts,
    src/components/audit-execution/engagements-table.tsx
  </files>
  <action>
    1. Add `getEngagements(session)` function to src/data-access/audit-execution.ts:
       - Query AuditEngagement with WHERE tenantId = session.user.tenantId
       - Include branch (name, code, city), auditPlan (year, quarter), teamMembers (user name/email)
       - Order by createdAt DESC
       - Return typed array

    2. Create src/components/audit-execution/engagements-table.tsx:
       - Client component ("use client")
       - Table with columns: Branch, Status (badge), Start Date, End Date, Team Members count, Created
       - Row click → router.push(`/audit-execution/${engagement.id}`)
       - Follow FindingsTable pattern (shadcn Table component)
       - Status badge colors: PLANNED=blue, IN_PROGRESS=yellow, COMPLETED=green, CANCELLED=red
       - Empty state: "No audit engagements yet. Create your first engagement."

  </action>
  <verify>
    - TypeScript compiles without errors
    - getEngagements function follows existing DAL pattern (prismaForTenant, tenantId WHERE clause)
    - EngagementsTable matches FindingsTable component pattern
  </verify>
  <done>AC-1 partially satisfied: Data layer and component ready</done>
</task>

<task type="auto">
  <name>Create /audit-execution index page and /admin hub page</name>
  <files>
    src/app/(dashboard)/audit-execution/page.tsx,
    src/app/(dashboard)/admin/page.tsx,
    src/lib/nav-items.ts
  </files>
  <action>
    1. Create src/app/(dashboard)/audit-execution/page.tsx:
       - Server component following findings/page.tsx pattern
       - requirePermission("audit_execution:read")
       - Fetch engagements via getEngagements(session)
       - Header: "Audit Execution" title + subtitle with count
       - Summary cards: Planned, In Progress, Completed, Cancelled (4 cols)
       - "Create Engagement" button → Link to /audit-execution/create
       - EngagementsTable component below cards
       - Use icons from @/lib/icons (ClipboardList, Activity, CheckCircle2, etc.)

    2. Create src/app/(dashboard)/admin/page.tsx:
       - Server component following admin/users/page.tsx pattern
       - requirePermission("admin:manage_users")
       - Hub page with cards for each admin section:
         - Users (Users icon) → /admin/users: "Manage user accounts and roles"
         - Branches (Building2 icon) → /admin/branches: "Manage branch offices"
         - Zones (Map icon) → /admin/zones: "Manage audit zones"
         - Templates (FileText icon) → /admin/templates: "Manage observation templates"
         - RAM Config (Settings icon) → /admin/ram-config: "Configure risk assessment parameters"
       - Use shadcn Card components with icon + title + description + Link
       - Grid: 2 cols mobile, 3 cols desktop

    3. Fix sidebar admin link in src/lib/nav-items.ts:
       - Change Admin href from "/admin/users" to "/admin"

  </action>
  <verify>
    - `pnpm build` compiles without errors (or pnpm dev renders both pages)
    - /audit-execution shows engagement table with summary cards
    - /admin shows hub with 5 section cards
    - Sidebar Admin link points to /admin
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: Both index pages work, sidebar fixed</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- prisma/schema.prisma — no schema changes
- Existing admin sub-pages (users, branches, zones, templates, ram-config)
- Existing audit-execution sub-pages ([engagementId], create, sections)
- src/components/findings/ — read-only pattern reference

## SCOPE LIMITS

- Only ISS-001, ISS-003, ISS-011 in this plan
- No compliance lifecycle fixes (Plan 02-02)
- No navigation CTAs between flow steps (Plan 02-04)
- No i18n translation additions (use English strings, add t() later)
  </boundaries>

<skills>
Required:
- /nextjs-developer — New pages with App Router (required per SPECIAL-FLOWS.md)
</skills>

<verification>
- [ ] /audit-execution returns 200 (not 404)
- [ ] /audit-execution shows engagement list with status cards
- [ ] /audit-execution table rows link to /audit-execution/{id}
- [ ] /admin returns 200 (not 404)
- [ ] /admin shows 5 section cards
- [ ] Each admin card links to correct sub-page
- [ ] Sidebar Admin link points to /admin
- [ ] No TypeScript errors
</verification>

<success_criteria>

- Both /audit-execution and /admin return 200 with proper content
- Sidebar Admin link fixed
- All 3 issues (ISS-001, ISS-003, ISS-011) resolved
  </success_criteria>

<output>
Summary: `.paul/phases/02-bug-fixes/02-01-SUMMARY.md`
</output>
