---
phase: 03-demo-ready-polish
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/app/error.tsx
  - src/lib/nav-items.ts
  - CLAUDE.md
autonomous: true
---

<objective>
## Goal
Final cleanup pass: fix stale icon import, differentiate duplicate sidebar icons, and update CLAUDE.md Known Issues to reflect current state.

## Purpose

Root error.tsx imports directly from lucide-react (violates project convention). Two sidebar items share the same Shield icon. CLAUDE.md lists 3 issues that are already fixed, misleading anyone reading the docs.

## Output

- All icon imports use @/lib/icons barrel export
- Risk Management sidebar uses ShieldCheck (distinct from Compliance's Shield)
- CLAUDE.md Known Issues reflects actual current state
  </objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files

@src/app/error.tsx (line 14: imports AlertCircle from lucide-react)
@src/lib/nav-items.ts (lines 59, 129: both use Shield icon)
@CLAUDE.md (lines 250-256: Known Issues section)
</context>

<skills>
No specialized flows required.
</skills>

<acceptance_criteria>

## AC-1: Icon imports follow project convention

```gherkin
Given the project convention is to import icons from @/lib/icons
When reviewing src/app/error.tsx
Then AlertCircle is imported from @/lib/icons, not from lucide-react
```

## AC-2: Sidebar icons are visually distinct

```gherkin
Given Compliance and Risk Management are separate navigation sections
When viewing the sidebar
Then Compliance uses Shield and Risk Management uses ShieldCheck (visually distinct)
```

## AC-3: Known Issues reflect current state

```gherkin
Given Phase 2 fixed dashboard NaN (ISS-023) and missing index pages (ISS-001, ISS-003)
When reading CLAUDE.md Known Issues
Then fixed issues are removed and only genuine current issues remain
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix icon import and sidebar differentiation</name>
  <files>src/app/error.tsx, src/lib/nav-items.ts</files>
  <action>
    In `src/app/error.tsx`:
    - Change `import { AlertCircle } from "lucide-react"` to `import { AlertCircle } from "@/lib/icons"`

    In `src/lib/nav-items.ts`:
    - Change Risk Management icon from `Shield` to `ShieldCheck` (already imported on line 3)

  </action>
  <verify>pnpm build compiles without errors</verify>
  <done>AC-1 and AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md Known Issues</name>
  <files>CLAUDE.md</files>
  <action>
    Update the Known Issues section (lines 250-256):
    - Remove issue 1 (Dashboard NaN) — fixed in Plan 02-05 with null coalescing
    - Remove issue 3 (Missing index pages) — fixed in Plan 02-01 (/audit-execution and /admin pages created)
    - Keep issue 2 (SES sandbox) — still pending
    - Keep issue 4 (DB views not in migrations) — still true
    - Keep issue 5 (Seed data mismatch) — still true
    - Renumber remaining issues 1-3
  </action>
  <verify>CLAUDE.md Known Issues contains exactly 3 items (SES, DB views, seed mismatch)</verify>
  <done>AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- src/lib/icons.ts (no new exports needed)
- src/components/\* (no component changes)
- prisma/schema.prisma
- Any Phase 2 or Phase 3 Plan 01 work

## SCOPE LIMITS

- Only fix the specific items listed — no other CLAUDE.md edits
- Do NOT add new documentation files
- Do NOT refactor nav-items.ts beyond the icon change

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` compiles without TypeScript errors
- [ ] No imports from `lucide-react` in src/app/error.tsx
- [ ] Risk Management uses ShieldCheck in nav-items.ts
- [ ] CLAUDE.md Known Issues has 3 items (not 5)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.paul/phases/03-demo-ready-polish/03-02-SUMMARY.md`
</output>
